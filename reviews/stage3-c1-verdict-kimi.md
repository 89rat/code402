• I'll review both commits against the repo and vendored specs. Starting with the diffs.

• Now let me examine the surrounding code the diffs touch — `x402v2.rs` (validate, structural_gate), the error taxonomy, and lib.rs CORS handling.

• Let me verify a few specifics: wrangler vars, spec §5.1.2/§9 status codes, lib.rs routing, and the paying-crawler plan's C1 gate wording.

• Let me pin exact line numbers for citations.

• Findings:

  **MAJOR — Missing/malformed stamp extension returns 500, not a §9 4xx.** `crates/edge/src/x402v2_route.rs:168,173,177,189` — a spec-valid `PaymentPayload` that omits `extensions`, omits `mac`/`iat`, or carries a non-hex `mac` hits `Error::RustError(...)?` and propagates to workerd as a bare 500. This is a client fault mapped to a server fault: the vendored `specs/x402/http.md:180-186` table has 500 only for "internal server error during payment processing"; malformed payment is 400. It also contradicts the commit's own "taxonomy-coded errors throughout" claim. Note the official `@x402/fetch` client does not know `code402.stamp`; if its extension echo is ever lossy, every paid call from the official client 500s. `tests/v2-wire/run.sh` test 5 only covers undecodable base64 (which 400s a line earlier), so this path is untested.

  **MAJOR — `parse_v1_payment_required` cannot parse real x402 v1.** `crates/core/src/payment/x402v2_client.rs:40-75` deserializes v1 `accepts[]` directly into the v2-shaped `PaymentRequirements`, which requires field `amount` (`crates/core/src/payment/x402v2.rs:163`). Real v1 requirements carry `maxAmountRequired` — serde fails with missing-field on the first genuine v1 server. Even past that, real v1 networks are non-CAIP-2 (`"base-sepolia"`, `"base"`) and are rejected by `validate_spec`'s `is_caip2` (`x402v2.rs:200`). The test fixture (`crates/core/tests/x402v2_client.rs:48-58`) uses `"amount"` and `"eip155:84532"` — a v1 envelope with v2 field shapes — so C1 gate item "402 detection both forms (v2 header + real-v1 body)" is verified only against a v1 that looks like v2. Nothing in vendored `specs/` defines v1, so there is no in-repo fixture keeping this honest; the crawler's first wild v1 402 will fail detection.

  **MAJOR — Invalid-signature maps to HTTP 401, diverging from the vendored transport spec.** `crates/edge/src/x402v2_route.rs:201` (structural gate) and `:211` (prefilter). `specs/x402/http.md:180-186` defines no 401: invalid payment → 400, payment verification failed → 402. Official clients retry-with-payment specifically on 402; a 401 is a dead end. Compounding it, neither the 401s nor the route's 402s for `InvalidValidAfter`/`InvalidValidBefore` (`x402v2_route.rs:202-204`) carry a fresh `PAYMENT-REQUIRED` header, so a client that fails timing/signature has no protocol-level recovery path short of restarting the request from scratch.

  **MAJOR — /v2 drops the idempotency-replay short-circuit that /v1 has.** v1 checks the key before execution and returns the stored receipt (`crates/edge/src/lib.rs:332-344`); the v2 `serve()` only `INSERT OR IGNORE`s after execution and never reads (`x402v2_route.rs:322-328`). Same `idempotency_key` twice → two executions, two fresh responses, second insert silently ignored. Cross-file behavioral regression, and it removes the only client-side mitigation against the known-accepted replay-within-grace window (a retry with the same payment *and* same key is served twice).

  **MINOR — Stamp MAC does not bind the resource/route.** `canonical_requirement` MACs only `PaymentRequirements` (`x402v2_route.rs:64-66,186-189`); `resource.url` lives on `PaymentRequired`, and the gate checks `payload.resource.url` only "when present" (`crates/core/src/payment/x402v2.rs:606-610`) — the C1 client always sends `resource: None` (`x402v2_client.rs:187`). Two tools with identical pricing produce byte-identical requirements, so a stamp issued on `/v2/tools/A/call` verifies on `/v2/tools/B/call`. Distinct from the accepted same-route replay: cross-tool serving plus per-tool accounting drift. Gate item "resource.url match" is unenforced on the actual client path and has no test.

  **MINOR — CORS is promised but not delivered on actual responses.** Preflight sets `Access-Control-Allow-Origin: *` (`x402v2_route.rs:96`), but no 402/200/error response sets ACAO — `v2_err`/`cors` set only `Access-Control-Expose-Headers` (`x402v2_route.rs:34-48`). Browsers block reading the response entirely without ACAO on it, so the exposed PAYMENT-* headers are unreachable for browser clients. Additionally OPTIONS is answered before the `v2_enabled` check (`x402v2_route.rs:92-104` vs `:105`), so the "dark = indistinguishable from absent" claim is false for preflight — the 204 with payment Allow-Headers fingerprints the dark route in prod.

  **MINOR — `serve()` hardcodes the telemetry amount.** `x402v2_route.rs:303`: `let amount = 5000u64;` — any tool whose KV pricing differs gets mis-recorded in the events ledger while the stamped requirement governs the real charge.

  **MINOR — `SelectionPolicy::select` aborts instead of skipping, and mislabels denials.** `crates/core/src/payment/x402v2_client.rs:107`: a policy-matching but spec-invalid requirement returns `Err` immediately rather than trying later `accepts`. And every denial — wrong asset, over ceiling — surfaces as `X402Error::BadNetwork` (`:110`), which maps to `invalid_network` in the §9 taxonomy (`x402v2_errors.rs:78`): taxonomy drift for price/asset denials.

  **MINOR — Test coverage gaps against the stage's own gate list.** `tests/v2-wire/run.sh` has no negative test for: tampered-requirement MAC mismatch (G6 core property), stale-`iat` grace expiry, missing-stamp (the 500 above), or resource.url mismatch. The only inbound-negative tests are undecodable base64 and duplicate headers. Also `x402v2_route.rs:18` imports `encode_payment_signature` but never uses it — dead import.

  On the pending official-`@x402/fetch` e2e: acceptable for this gate — `payv2` drives the same core pipeline the mirror test validates, and the route is KV-dark in prod. But note `sdk/` contains zero v2 support, so no second, independent implementation has ever spoken this wire; the Stage 4 prod flip should carry the official-client e2e as a hard prerequisite, particularly because the stamp-echo (finding 1) and 401 mapping (finding 3) are exactly the behaviors only a third-party client will exercise.

