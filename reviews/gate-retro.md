# Gate retro — one line per gate (PANEL.md kaizen loop)

- Stage 1: taxonomy-code reachability never proven (exhaustive match satisfied the compiler, left two §9 codes dead) → catcher: reachability test class (every code reachable or facilitator-origin), standing pattern for enum↔enum mappings. [2026-08-19]
- Stage 3: e2e covered happy path + garbage, never the malformed-VALID-JSON middle (stamp fields) -> catcher: negative e2e class "spec-valid envelope, semantically-invalid payment" mandatory per gate. [2026-08-19]
- C1: fixtures written from our own types' assumptions (v1-shaped-v2) -> catcher: fixtures for FOREIGN formats derive from the foreign spec, never our types (standing rule). [2026-08-19]
- 2026-08-19 reconciler gate: the live e2e caught what unit tests couldn't (CDP's real already-used shape != volley shape) — lesson: facilitator-classification changes need a live-shape probe before the class is trusted; now caught by classify_settle_failure matrix + cdp-settle-raw.mjs probe. Kimi M1 leaked because the e2e repost was faster than the stamp grace — lesson: e2e timing-dependent passes must be annotated; now caught by the M1 fix + aged-repost in the regression corpus follow-up.
