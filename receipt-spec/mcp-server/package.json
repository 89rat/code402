{
  "name": "mcp-server-xdr1",
  "version": "0.1.0",
  "description": "MCP server: verify XDR-1 delivery receipts (offline commitment + signature, optional on-chain settlement check). Reference verifier for the XDR-1 spec.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "tsx index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
